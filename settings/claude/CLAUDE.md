When writing or changing code, when feasible, prove that it works. Add new tests when it makes sense. Otherwise, verify through methods.

You have access to Playwright MCP for testing in the browser.

Additional CLI tools you have access to anywhere in the system:

- uv
- jq
- Github CLI

Always prefer LSP over Grep for code navigation.
