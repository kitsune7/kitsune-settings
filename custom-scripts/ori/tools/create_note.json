{
  "type": "function",
  "function": {
    "name": "create_note",
    "description": "This tool creates a new markdown note in an Obsidian vault. It should be used when the user asks to create a note.",
    "parameters": {
      "type": "object",
      "properties": {
        "note_name": {
          "type": "string",
          "description": "The name of the note. It shouldn't contain a file extension."
        },
        "note_content": {
          "type": "string",
          "description": "The content for the note. It should be in markdown format."
        }
      },
      "required": ["note_name", "note_content"]
    }
  }
}
