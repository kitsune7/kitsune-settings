{
  "type": "function",
  "description": "Create a new note with specified name, location, and markdown contents.",
  "name": "create_note",
  "parameters": {
    "type": "object",
    "properties": {
      "note_name": {
        "type": "string",
        "description": "The name of the note."
      },
      "location": {
        "type": "string",
        "description": "The location where the note should be stored or associated with."
      },
      "markdown_contents": {
        "type": "string",
        "description": "The markdown content for the note."
      }
    },
    "required": ["note_name", "location", "markdown_contents"]
  },
  "returns": {
    "type": "object",
    "properties": {
      "note_id": {
        "type": "string",
        "description": "A unique identifier for the newly created note."
      }
    }
  }
}
